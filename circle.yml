machine:
  java:
    version: oraclejdk8
  timezone:
    Europe/Berlin

dependencies:
  override:
    - chmod +x gradlew
    - ./gradlew dependencies
  cache_directories:
    - "~/.gradle"

test:
  override:
    - chmod +x gradlew
    - ./gradlew test
    - ./gradlew pitest