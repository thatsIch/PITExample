machine:
  java:
    version: oraclejdk8
  timezone:
    Europe/Berlin

dependencies:
  override:
    - chmod +x gradlew
    - ./gradlew dependencies

test:
  override:
    - chmod +x gradlew
    - ./gradlew clean assemble test pitest